{
  "name": "@air/daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": { "@air/server": "workspace:*" },
  "scripts": { "dev": "bun --watch src/index.ts", "build": "bun build src/index.ts --outdir dist --target bun", "typecheck": "tsc -p tsconfig.json --noEmit" }
}
